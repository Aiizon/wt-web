<nav class="navbar navbar-expand-lg navbar-dark fixed-top p-4 bg-dark">
    <div class="d-flex flex-row justify-content-evenly gap-4 container-fluid">
        <a class="navbar-brand" href="{{ path('home') }}">
            <img src="{{ asset('images/icon.png') }}" alt="logo de Work Together" width="30" class="d-inline-block align-text-top rounded-1">
            Work Together
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
            <ul class="navbar-nav">
                <li class="d-flex flex-row align-items-center justify-content-center nav-item">
                    <a class="nav-link" href="{{ path('home') }}">Accueil</a>
                </li>

                {% if is_granted('ROLE_USER') %}
                    <li class="d-flex flex-row align-items-center justify-content-center nav-item">
                        <a class="nav-link" href="{{ path('logout') }}">Déconnection</a>
                    </li>
                    <hr class="vr bg-dark-subtle mx-2 d-none d-md-block"/>
                    <hr class="bg-dark-subtle mx-2 d-sm-none d-block"/>
                    {% if app.user.picturePath %}
                        <img src="/images/upload/{{ app.user.picturePath }}" alt="" width="30" height="30" class="rounded overflow-hidden align-self-center">
                    {% endif %}
                    <li class="d-flex flex-row align-items-center justify-content-center nav-item">
                        <a class="nav-link text-center" href="{{ path('customer_area_home') }}">Espace<br/>utilisateur</a>
                    </li>
                {% else %}
                    <li class="d-flex flex-row align-items-center justify-content-center nav-item">
                        <a class="nav-link" href="{{ path('login') }}">Connection</a>
                    </li>
                    <li class="d-flex flex-row align-items-center justify-content-center nav-item">
                        <a class="nav-link" href="{{ path('register') }}">Inscription</a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>